id: 04-inject-mutation
trace: eval-04-inject-mutation
seed: seed/
task:
  title: "Add sum(a, b) via inject-mutation"
  body: |
    Implement Sum in the seeded package so tests pass.
  bee: builder
  intent: test-fix
  review: none
fault:
  mode: inject-mutation
oracle:
  command: "go test ./pkg/..."
  workdir: "."
  max_rework_cycles: 2
  expect_event_chain:
    - type: MUTATION
      kind: code.proposal.isolated
    - type: VERIFICATION
      kind: verification.failed
    - type: MUTATION
      kind: code.proposal.isolated
    - type: VERIFICATION
      kind: verification.success
score:
  must_pass_tests: true
  expect_task_status: completed
  max_builder_runs: 2
timeout: 10m
