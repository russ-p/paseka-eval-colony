id: 13-artifact-deferred-skip
trace: eval-13-artifact-deferred-skip
seed: seed/
# Spec 014 §9 + 015: deferred artifact.written skips duplicate scan flush
# even when scan-extra.md would otherwise be announced.
task:
  title: "Add sum with deferred artifact.written"
  body: |
    Implement Sum in the seeded package so tests pass.
  bee: builder
  intent: test-fix
  review: none
fault:
  mode: deferred_artifact
oracle:
  command: "go test ./pkg/..."
  workdir: "."
  max_rework_cycles: 0
  expect_event_chain:
    - type: SIGNAL
      kind: artifact.written
    - type: INSIGHT
      kind: run.summary
    - type: MUTATION
      kind: code.proposal.isolated
    - type: VERIFICATION
      kind: verification.success
score:
  must_pass_tests: true
  expect_task_status: completed
  max_builder_runs: 1
  expect_artifact_written_count: 1
timeout: 10m
